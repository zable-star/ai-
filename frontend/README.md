# Frontend

Browser-only voice drawing application.

## Run

From the repository root:

```powershell
python -m http.server 8000
```

Open:

```text
http://localhost:8000/frontend/
```

## Notes

The app uses browser speech recognition. Chrome or Edge is recommended.

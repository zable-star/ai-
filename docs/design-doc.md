# 设计文档：AI 语音绘图工具

## 1. 题目选择

本项目选择题目二：AI 语音绘图工具。

目标是让用户不依赖鼠标或键盘，仅通过语音完成基础绘图创作。浏览器首次获取麦克风权限可能需要一次点击，这是浏览器安全限制；进入绘图流程后，绘图、修改、撤销、清空和导出都通过语音完成。

## 2. 计划支持的指令能力

| 能力 | 计划内容 |
| --- | --- |
| 基础图形 | 圆形、矩形、三角形、直线、星形、点 |
| 复合对象 | 房子、树、太阳、云朵、河流、山、花、笑脸等常见语义对象 |
| 颜色控制 | 红、蓝、绿、黄、紫、黑、白、灰、橙、粉 |
| 位置控制 | 中间、左上、右上、左下、右下、上方、下方、左边、右边 |
| 线宽控制 | 通过“线宽五”“粗一点”“细一点”调整 |
| 画布控制 | 清空、撤销、重做、保存图片 |
| 复杂指令 | 用“然后、再、并且”等词拆分多步命令 |
| 场景模板 | 通过“画一幅风景/花园/夜空”等指令快速生成多步作品 |
| 局部编辑 | 支持“把上一笔改成蓝色/移到右上角/放大一点”等引用上一笔的编辑 |
| 视图控制 | 支持通过按钮或语音放大、缩小、适应画布视图 |
| 容错表达 | 支持部分近义词，例如“圆/圆形/圈”“矩形/方形” |
| 响应反馈 | 展示每条语音指令的拆解步骤、执行来源和耗时 |

## 3. 已实现的指令能力

| 能力 | 当前实现情况 |
| --- | --- |
| 基础图形 | 已实现圆形、矩形、三角形、直线、星形、点 |
| 复合对象 | 已实现房子、树、太阳、云朵、河流、山、花、笑脸的 Canvas 组合绘制 |
| 颜色控制 | 已实现常用中文颜色和部分英文颜色 |
| 位置控制 | 已实现九宫格位置表达 |
| 线宽控制 | 已实现固定线宽和粗细调节 |
| 画布控制 | 已实现清空、撤销、重做、保存图片 |
| 复杂指令 | 已实现多步拆分、复合对象拆解和顺序执行 |
| 场景模板 | 已实现风景、花园、夜空模板，自动拆成背景、物体和线条动作 |
| 局部编辑 | 已实现对上一笔绘图动作的颜色、位置、大小和线宽编辑 |
| 视图控制 | 已实现响应式布局、画布自适应、视图放大/缩小/适应窗口 |
| 反馈 | 已实现识别文本、执行日志、语音播报、绘图计划和解析耗时展示 |
| 账号 | 已实现浏览器本地注册、登录和退出 |
| 标签页 | 已实现左侧绘图、账号、大模型、帮助标签 |
| 大模型 | 已实现 DeepSeek、OpenAI、通义千问等服务商预设、其他 OpenAI-compatible Endpoint、多配置保存和本地规则失败后的模型规划，支持返回复合对象动作 |
| 导出 | 已实现 PNG、JPEG、WebP 格式导出 |
| 测试 | 已实现命令解析自测 |

## 4. 未完成能力与原因

| 未完成能力 | 原因 |
| --- | --- |
| 完全自由语义绘图 | 需要云端大模型或图像生成模型，成本和不确定性更高，不适合作为第一版核心路径 |
| 像素级编辑 | 需要更复杂的对象选择、坐标推理和编辑模型，超出 72 小时 MVP 范围 |
| 任意对象选择编辑 | 当前已支持上一笔编辑；“把左边第二棵树变大”这类任意对象选择仍需更强的对象索引和引用解析 |
| 多语言语音识别 | 当前优先中文命令，英文仅做少量颜色兼容 |
| 服务端账号系统 | 当前部署在 GitHub Pages，没有安全后端；本地账号用于演示用户流程，生产版需要托管认证服务 |
| 服务端 API 代理 | 目前由用户在浏览器输入自己的 API Key；生产版应通过后端代理隐藏密钥 |
| 素材库 | 暂定为下一步扩展。当前优先保证语音绘图、模型接入、账号隔离和导出链路稳定；素材库会引入图片版权、加载性能和画布合成复杂度 |

## 5. 指令理解与容错策略

- 将一句话按“然后、接着、再、并且”等连接词拆分为多个子命令。
- 每个子命令先识别控制类动作，如清空、撤销、重做、保存。
- 再识别颜色、线宽、位置和图形。
- 对常见同义表达做词典映射。
- 对常见语义对象做本地映射，例如“房子、小屋、树、太阳、河流、云朵”等，不依赖云端即可绘制组合图形。
- 对常见场景做模板化拆解，例如“画一幅风景”会拆成背景、云、太阳、山、房子、树、河流等步骤。
- 对“刚才、上一笔、最后一个”等引用做轻量解析，可编辑最近的绘图动作。
- 对“放大画布、缩小画布、画布适应窗口”等命令做视图控制，不影响真实导出分辨率。
- 无法识别时给出可恢复反馈，不改变画布。
- 当本地规则无法识别且用户启用大模型时，调用 OpenAI-compatible API，将自然语言转换为结构化动作 JSON。
- 大模型配置与本地账号绑定。未登录或退出后不会加载 API Key；重新登录后才恢复该账号保存的模型配置。
- 配置界面支持保存多个模型配置，用户可以按服务商或用途灵活切换。
- 模型返回结果会经过类型、坐标、颜色、尺寸和线宽校验，避免无效输出直接执行。
- 绘图界面展示“本地解析”或“AI 规划”的动作列表和耗时，便于观察拆解过程和响应延迟。

## 6. 语音到绘图延迟策略

- 使用浏览器 Web Speech API 获取最终识别文本。
- 最终文本一旦返回，立即在本地解析并绘制。
- 本地可识别命令不调用云端模型，避免网络等待。
- 只有本地规则全部失败且用户已启用 API 配置时才调用云端模型规划。
- 每次语音指令都会记录解析来源、动作步数和毫秒级耗时，便于调试延迟瓶颈。
- Canvas 渲染只重放动作历史，数据量小，响应稳定。
- 前端布局会根据浏览器宽高自适应，画布保持 16:9 并尽量利用可用空间。

## 7. 复杂指令拆解与执行

示例：

```text
画一幅风景，然后把上一笔放大一点
```

执行过程：

1. “画一幅风景”先匹配本地场景模板。
2. 模板拆成背景、云、太阳、山、房子、树、河流等结构化动作。
3. “把上一笔放大一点”解析为 editLast 动作。
4. 执行器定位最近的绘图动作，复制并修改大小，再重绘画布。

## 8. 成本控制

虽然题目二没有强制要求端云成本说明，但本项目仍把成本控制作为设计目标：

- MVP 不使用云端大模型，避免按 token 或音频计费。
- 不上传画布内容，减少存储和带宽成本。
- 只在浏览器内保存绘图历史，避免服务器资源消耗。
- 后续如果接入云端语义解析，可采用“本地规则优先，失败后再调用模型”的混合策略。
- 当前已采用混合策略：能由本地规则理解的命令不调用模型；只有未知复杂命令才进入模型规划。
- 已将房子、树、太阳、云朵、河流等常见对象放到本地组合绘制，减少为简单场景调用模型的次数。
- 已将风景、花园、夜空等高频场景模板放在本地拆解，进一步减少模型调用。
- API Key 由用户自己填写并保存在当前登录账号的本地数据中，不写入仓库；退出登录后运行态不再持有该配置。
- 通过多个配置档切换不同服务商，便于在 DeepSeek、OpenAI、通义千问等模型之间按效果和成本选择。

## 9. 后续扩展

- 引入任意对象选择：例如“把左边的圆移动到右边”。
- 引入云端命令规划器：将复杂自然语言转为操作 JSON。
- 扩展模板化作品：流程图、海报草图、教学板书。
- 支持导出操作历史，便于复现绘图过程。
- 评估素材库：支持快速加载图片、模板和背景，但需要先解决素材版权、移动端加载性能、图片与语音操作的组合方式。
- 将本地账号升级为后端账号系统，并把模型 API 调用迁移到后端代理。

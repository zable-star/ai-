# Backend

No backend is required for the current MVP.

This directory is reserved for future extensions:

- Cloud command planner
- Shared drawing sessions
- Telemetry and evaluation
- Server-side export
